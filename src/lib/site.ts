/* URL de producción del sitio.
   Única fuente de verdad: la usan el metadata (metadataBase, que resuelve
   las imágenes de Open Graph) y el sitemap. Si algún día se pasa a un
   dominio propio, se cambia acá y nada más. */
export const SITE_URL = "https://distribucionargentina.vercel.app";
